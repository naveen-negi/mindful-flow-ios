# RevenueCat public SDK key for the iOS app (App Store Connect app → RevenueCat → API keys).
# Copy to .env.production before `npm run build && npx cap sync ios`.
# Without it the app runs entirely on the free tier and the Pro screen says "Available in the iOS app".
VITE_REVENUECAT_IOS_KEY=appl_xxxxxxxxxxxxxxxxxxxxxxxx
